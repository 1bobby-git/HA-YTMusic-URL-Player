# HA-YTMusic-URL-Player 1.9.4 — 최적화

2026-09-06 / Home Assistant 통합 패치

## 변경 내용

- 동일 영상의 동시 캐시 미스에서 성공한 추출 결과 재사용; 영상별 잠금은 유휴 시 자동 제거
- 메타데이터 캐시를 128개로 제한하고 시스템 시각 변경의 TTL 영향 제거
- 긴 재생을 중단시키는 전체 30초 제한 대신 연결/수신 정체 30초 제한
- 이미 전송을 시작한 스트림에서 오류가 나면 새 응답·추출을 반복하지 않음
- 추출기 선택·재생목록·서비스·엔티티·인증 정책 및 라이브러리 버전은 변경하지 않음

## 호환성과 범위

도메인, 기존 설정, 엔티티·장치 식별자와 공개 서비스 이름은 유지합니다. 의존성 일괄 업그레이드와 서버 측 세션 만료 우회는 포함하지 않습니다. SmartThings 브리지 앱과 ANCS 펌웨어는 변경하지 않습니다.

## 검증 범위

원본 전체 파일 해시를 확인한 뒤 기존 저장소 테스트를 변경 전후 실행합니다. HACS/Hassfest와 저장소별 CI 검증을 통과한 경우에만 릴리스를 게시합니다. 앞선 후보 묶음의 오프라인 테스트 103개와 실제 저장소 테스트 결과는 별개입니다. 운영 HA의 실제 로그인·쿠키 수명·재생·실기기 제어·장시간 운용은 검증하지 않았습니다. 테스트 통과만으로 모든 환경에서 무영향이나 성능 향상을 보장하지 않습니다.

## 업데이트와 되돌리기

HA 백업을 확보한 후 HACS에서 1.9.4을 내려받고 Home Assistant를 재시작합니다. 문제가 생기면 HACS 재다운로드에서 이전 버전 1.9.3을 선택하거나 백업을 복원합니다. 이 GitHub 배포 작업은 사용자 HA에 직접 설치하거나 재시작하지 않습니다.

기준 커밋: `de8259eb03c8131c9580a91cebd3a1d300a5461d`

배포 검증 실행: https://github.com/1bobby-git/HA-YTMusic-URL-Player/actions/runs/33999503206

## 실제 CI 결과

기존 코드: {'tests': 13, 'failures': 0, 'errors': 0, 'skipped': 0}

패치 코드: {'tests': 13, 'failures': 0, 'errors': 0, 'skipped': 0}

HACS와 Hassfest 검증을 통과한 뒤 게시합니다.
